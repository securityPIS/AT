# `LoadingScreen.jsx`

**Tujuan:** Layar loading saat auth / data awal belum siap (logo Pertamina + spinner).

Ukuran berkas ~13 baris.

## Ekspor
- **default**: `LoadingScreen`

## Props

Tidak ada — komponen statis.

## Catatan maintenance
- Hasil refactor ronde 2 dari `app.jsx` (lihat `SYSTEM_MAP.md`).
- Komponen presentasional — state & handler tetap di `app.jsx`.
